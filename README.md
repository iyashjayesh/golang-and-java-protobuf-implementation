# Protobuf-Golang-Example
Protobuf Golang Implementation Example - With Cross Language Message (Golang)

Use the following command to generate the go code for the proto file
```protoc --go_out=. *proto```
